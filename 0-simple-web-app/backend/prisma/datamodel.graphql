type User {
  id: ID! @unique
  email: String! @unique
  events: [Event!]!
}

type Event {
  id: ID! @unique
  category: EventCategory!
  user: User!
  place: String!
  address: String!
}

type EventCategory {
  id: ID! @unique
  name: String! @unique
}